db:
  build: ./db
  environment:
    POSTGRES_USER: docker
    POSTGRES_PASSWORD: docker123
  ports:
    - "5432:5432"

api:
  build: ./api
  links: 
    - db
  ports:
    - "127.0.0.1:8088:8080"

parser:
  build: ./parser
  links:
    - db
